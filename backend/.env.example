# Environment variables for EventSnap Backend

# Server Configuration
PORT=5000
NODE_ENV=development

# Database
# For local development:
MONGODB_URI=mongodb://localhost:27017/eventsnap
# For production (MongoDB Atlas):
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/eventsnap?retryWrites=true&w=majority

# JWT Secret (IMPORTANT: Generate a strong secret for production!)
# Generate with: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_here_change_in_production

# Cloudinary Configuration (Get these from https://cloudinary.com/console)
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# Frontend URL (for CORS configuration)
# For local development:
FRONTEND_URL=http://localhost:3000
# For production (Vercel):
# FRONTEND_URL=https://your-app-name.vercel.app

# Gmail Configuration (for sending QR codes via email)
GMAIL_USER=your_email@gmail.com
GMAIL_APP_PASSWORD=your_gmail_app_password

# Deployment Notes:
# 1. Never commit your actual .env file to version control
# 2. Use different secrets for development and production
# 3. Set these as environment variables in your deployment platform (Render, Heroku, etc.)
# 4. Update FRONTEND_URL to match your Vercel deployment URL
# 5. Update MONGODB_URI to your MongoDB Atlas connection string for production