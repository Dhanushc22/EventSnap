# EventSnap Backend - Production Environment Variables
# Set these in your Render.com dashboard

# Server Configuration
PORT=5000
NODE_ENV=production

# Frontend URL - CRITICAL for QR codes
# Replace with your actual Vercel deployment URL
FRONTEND_URL=https://eventsnap.vercel.app

# Database - MongoDB Atlas connection string
# Format: mongodb+srv://username:password@cluster.mongodb.net/eventsnap?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://your-username:your-password@cluster0.xxxxx.mongodb.net/eventsnap?retryWrites=true&w=majority

# JWT Secret - Generate with: openssl rand -base64 32
JWT_SECRET=your-production-jwt-secret-change-this-to-random-value

# File Upload Directory
UPLOAD_DIR=uploads

# CORS Configuration - Only allow these origins
CORS_ALLOWED_ORIGINS=https://eventsnap.vercel.app,http://localhost:3000

# Cloudinary Configuration (for image storage)
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# Email Configuration (Gmail SMTP)
GMAIL_USER=dhanush.c.s.dev@gmail.com
GMAIL_APP_PASSWORD=lqzy dfbu iofa igcc

# Production Notes:
# 1. Replace ALL placeholder values with actual production values
# 2. Never commit this file to version control
# 3. Set these variables in Render.com dashboard, not in a file
# 4. Generate strong, unique JWT_SECRET for production
# 5. Use MongoDB Atlas for production database